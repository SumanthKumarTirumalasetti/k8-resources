kind: Service
apiVersion: v1
metadata:
  name: nginx
spec:
  selector:
    project: expense
    component: backend
    environment: dev
  ports:
  - port: 80
    targetPort: 80

---

kind: Pod
apiVersion: v1
metadata:
  name: backend
  labels:
    project: expense
    component: backend
    environment: dev
spec:
  containers:
  - name: nginx
    image: nginx
